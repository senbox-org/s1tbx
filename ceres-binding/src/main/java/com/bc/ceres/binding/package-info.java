/**
 * A simple data binding framework for binding Java objects to a GUI or a DOM.
 * <p>This package and its sub-packages currently form a self-standing API which is
 * not used by <i>Ceres</i> itself.</p>
 * <p>An important entry point into this API may be the {@link ValueContainer} class, which
 * is used to create {@link ValueModel}s from diverse sources.</p>
 * @since Ceres 0.6
 */
package com.bc.ceres.binding;
